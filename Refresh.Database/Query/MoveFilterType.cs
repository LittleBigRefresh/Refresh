namespace Refresh.Database.Query;

public enum MoveFilterType
{
    /// <summary>
    /// Include move levels in the results
    /// </summary>
    True,
    /// <summary>
    /// Only include move levels in the results
    /// </summary>
    Only,
    /// <summary>
    /// Do not include move levels in the results
    /// </summary>
    False,
}